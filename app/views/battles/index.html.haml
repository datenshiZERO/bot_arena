%h1 Battle List

%table.table.table-striped.table-condensed
  - @battles.each do |battle|
    %tr
      %td
        = link_to "#{battle.battle_id}", battle
      %td.text-right
        = l battle.updated_at, format: :short

= paginate @battles, theme: 'twitter-bootstrap-3'
