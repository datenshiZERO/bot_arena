%h1 Battle List

%table.table.table-striped.table-condensed
  - @battles.each do |battle|
    %tr
      %td
        = link_to "#{battle.arena.name}##{battle.id}", battle
      %td.text-right
        = battle.updated_at

= paginate @battles, theme: 'twitter-bootstrap-3'
