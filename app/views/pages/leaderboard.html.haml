%h1 Leaderboard

%h2 Top Ten Players
.row
  .col-md-6
    %table.table.table-condensed.table-striped
      %tr
        %th Player
        %th.text-right Trophies
      - @top_ten.each do |user|
        %tr
          %td
            = user.username
            = user_flair(user)
          %td.text-right
            = number_with_delimiter user.total_trophies
            %span.fa.fa-trophy

      

